{{- if .Values.executor.service.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "spiderkeeper-executor.fullname" . }}
  labels:
    {{- include "spiderkeeper.labels" . | nindent 4 }}
    component: "{{ .Values.executor.name }}"
    app.kubernetes.io/component: "{{ .Values.executor.component }}"
spec:
  type: {{ .Values.executor.service.type }}
  ports:
    - port: {{ .Values.executor.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spiderkeeper.selectorLabels" . | nindent 4 }}
    component: "{{ .Values.executor.name }}"
{{- end}}