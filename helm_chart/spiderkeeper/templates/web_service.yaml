{{- if .Values.web.service.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "spiderkeeper-web.fullname" . }}
  labels:
    {{- include "spiderkeeper.labels" . | nindent 4 }}
    component: "{{ .Values.web.name }}"
    app.kubernetes.io/component: "{{ .Values.web.component }}"
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spiderkeeper.selectorLabels" . | nindent 4 }}
    component: "{{ .Values.web.name }}"
{{- end}}